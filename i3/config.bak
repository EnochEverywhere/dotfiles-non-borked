# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:System San Francisco Display 12
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec st

# kill focused window
bindsym $mod+q kill


bindsym $mod+space exec rofi -show drun -font 'System San Francisco Display 13'
bindsym $mod+o exec rofi -show window -font 'System San Francisco Display 13'
bindsym $mod+Escape exec  rofi -show power-menu -modi power-menu:/home/nightss/.local/bin/rofi-power-menu -font 'System San Francisco Display 13'

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h
# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+d floating toggle

# change focus between tiling / floating windows
bindsym $mod+d focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child


# assign workspaces to screens

workspace "1:Ultrawide" output DisplayPort-1
workspace "2:Left" output DVI-1
workspace "3:Vertical" output DVI-0
workspace "4:Ultrawide2" output DisplayPort-1
workspace "5:Vertical2" output DVI-0
workspace "6:Left 2" output DVI-1
workspace "7:Left 3" output DVI-1
workspace "8:Vertical" output DVI-0
workspace "9:Torrent" output DisplayPort-1
workspace "10 Ultrawide 4" output DisplayPort-1

set $workspace1 "1:Ultrawide"
set $workspace2 "2:Left"
set $workspace3 "3:Vertical"
set $workspace4 "4:Ultrawide2"
set $workspace5 "5:Vertical2"
set $workspace6 "6:Left 2"
set $workspace7 "7:Left 3"
set $workspace8 "8:Vertical"
set $workspace9 "9:Torrent"
set $workspace10 "10 Ultrawide 4"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10

#make apps launch on spesific workspaces

assign [class="Deadbeef"]      $workspace3
assign [class="Steam"]         $workspace4
assign [class="discord"]       $workspace5
assign [class="Gimp"]          $workspace10
assign [class="Deluge"]        $workspace9
assign [instance="cmus"]       $workspace3

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Escape exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Set colors from Xresources
# Change 'color7' and 'color2' to whatever colors you want i3 to use
# from the generated scheme.
# NOTE: The '#f0f0f0' in the lines below is the color i3 will use if
# it fails to get colors from Xresources.
set_from_resource $fg i3wm.color15 #ff2121
set_from_resource $bg i3wm.color5 #ff2121
set_from_resource $sg i3wm.color10 #ff2121



# class                 border  backgr. text indicator child_border
client.focused          $bg     $bg     $fg  $sg       $bg
client.focused_inactive $fg     $bg     $fg  $sg       $bg
client.unfocused        $fg     $fg     $fg  $sg       $fg
client.urgent           $bg     $bg     $fg  $sg       $bg
client.placeholder      $bg     $bg     $fg  $sg       $bg

client.background       $bg

# PROTIP: You can also dynamically set dmenu's colors this way

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks -c /home/nightss/i3blocks.conf
                colors {
			background $bg
	    	separator  $fg
		#                  border             background         text
		focused_workspace  $bg                $fg                $sg
		inactive_workspace $fg                $bg                $fg
		urgent_workspace   $fg                $fg                $bg
	}
    position top
output Displayport-1
font pango:System San Francisco Display 13
mode hide
    hidden_state hide
    modifier off

}

bar {
        status_command i3blocks -c /home/nightss/DVI.1.conf
        colors {
			background $bg
	    	separator  $fg
		#                  border             background         text
		focused_workspace  $bg                $fg                $sg
		inactive_workspace $fg                $bg                $fg
		urgent_workspace   $fg                $fg                $bg
	}
    position top
output DVI-1
tray_output |primary|<Displayport-1>
font pango:System San Francisco Display 13
mode hide
    hidden_state hide
    modifier off


}
bar {
        status_command i3blocks -c /home/nightss/DVI-0.conf
        colors {
			background $bg
	    	separator  $fg
		#                  border             background         text
		focused_workspace  $bg                $fg                $sg
		inactive_workspace $fg                $bg                $fg
		urgent_workspace   $fg                $fg                $bg
	}
    position top
output DVI-0
tray_output |primary|<Displayport-1>
font pango:System San Francisco Display 13
mode hide
    hidden_state hide
    modifier off

}

#Display Settings

exec_always xrandr --output DisplayPort-0 --off --output DisplayPort-1 --primary --mode 2560x1080 --pos 3120x568 --rotate normal --output HDMI-0 --off --output DVI-0 --mode 1920x1200 --pos 0x0 --rotate left --output DVI-1 --mode 1920x1080 --pos 1200x568 --rotate normal


#Wallpaper Settings

#custom keybinds to open programs

bindsym $mod+b exec brave
bindsym $mod+shift+Return exec terminator
bindsym $mod+u exec urxvt --name protonvpn-cli c -e protonvpn-cli c
bindsym $mod+shift+u exec urxvt --name protonvpn-cli d -e protonvpn-cli d
bindsym $mod+shift+t exec urxvt --name protonvpn-cli s -e protonvpn-cli s -c 'protonvpn-cli s & sleep 60'
#bindsym $mod+shift+g exec gtk-launch deadbeef.desktop
bindsym $mod+shift+g exec urxvt --name ranger -e ranger
bindsym $mod+m bar hidden_state toggle
bindsym $mod+F8 exec urxvt --name cmus -e cmus
bindsym $mod+p exec cmus-remote --pause
bindsym $mod+n exec cmus-remote --next
bindsym $mod+shift+v exec urxvt --name vim -e vim ~/.i3/config
bindsym $mod+shift+F7 exec urxvt --name vim -e vim /home/nightss/.config/surfraw/urls
bindsym $mod+shift+space exec /home/nightss/.local/bin/bookmarker
bindsym $mod+shift+b exec qutebrowser
bindsym $mod+shift+n exec /home/nightss/.local/bin/ddict
bindsym $mod+shift+y exec /home/nightss/.local/bin/ddyt
bindsym $mod+shift+i exec /home/nightss/.local/bin/searcher
bindsym $mod+F5 exec /home/nightss/.local/bin/volume
bindsym $mod+F12 exec rofi-tpb search
bindsym $mod+F4 exec /home/nightss/.local/bin/rofi-cmus
bindsym $mod+y exec ytfzf -D
bindsym $mod+g exec st -e newsboat
bindsym $mod+F10 exec  rofi -font 'System San Francisco Display 13' -show filebrowser


# Media volume controls
bindsym XF86AudioMute exec amixer sset 'Master' toggle
bindsym $mod+F6 exec amixer sset 'Master'
bindsym $mod+F7 exec amixer sset 'Master' 5%+

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

#float windows

for_window [class="systemsettings"] floating enable
for_window [class="Nemo"] floating disable
for_window [class="VLC"] floating enable
for_window [class="ark"] floating enable
for_window [class="Font-manager"] floating enable
for_window [class="feh"] floating enabled
for_window [class="File-roller"] floating enabled
for_window [instance="sxiv"] floating enabled
for_window [title="alsamixer"] floating enabled

#no focus

#no_focus [class="Sxiv"]
#for_window [class="Sxiv"] resize set  resize set 300 300

# Border
for_window [class=".*"] border pixel 2

#global gaps

gaps inner 10
gaps outer 15

#workspace gaps
workspace $workspace1 gaps inner 0 gaps outer 0

#pywal settings

exec_always --no-startup-id wal -R

exec picom --experimental-backends
exec nm-applet -b

################################################################################################
## sound-section - DO NOT EDIT if you wish to automatically upgrade Alsa -> Pulseaudio later! ##
################################################################################################

exec --no-startup-id volumeicon
bindsym $mod+Shift+m exec terminal -e 'alsamixer'
#exec --no-startup-id pulseaudio
#exec --no-startup-id pa-applet
#bindsym $mod+Shift+m exec pavucontrol

